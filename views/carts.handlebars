<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{{title}}</title>
</head>
<body>
    <h1>{{title}}</h1>
    <h2>Cart ID: {{cart.id}}</h2>
    <p>User ID: {{cart.userId}}</p>
    <ul>
        {{#each cart.products}}
            <li>Product ID: {{this.productId}}, Quantity: {{this.quantity}}</li>
        {{/each}}
    </ul>
</body>
</html>
